{
  "version": 3,
  "build": {
    "env": {},
    "command": "npm run build --prefix frontend",
    "outputDirectory": "frontend/dist"
  },
  "routes": [
    { "src": "/(.*)", "dest": "frontend/$1" }
  ]
}
